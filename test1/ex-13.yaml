apiVersion: v1
kind: Pod
metadata:
  labels:
    run: ex-13
  name: ex-13
spec:
  containers:
  - image: nginx:alpine
    name: ex-13
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
