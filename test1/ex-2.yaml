apiVersion: v1
kind: Pod
metadata:
  labels:
    run: ex-2
  name: ex-2
spec:
  containers:
  - image: nginx:alpine
    name: ex-2
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
