apiVersion: v1
kind: Pod
metadata:
  labels:
    run: ex-15
  name: ex-15
spec:
  containers:
  - image: nginx
    name: ex-15
    command: ["sleep"]
    args: ["3600"]
    ports:
    - containerPort: 80
  dnsPolicy: ClusterFirst
  restartPolicy: Always
